cmake_minimum_required (VERSION 2.6)
project (SmartFridge)

#configure_file(
#"${PROJECT_SOURCE_DIR}/"
#)

#  Add binary tree to search file 
#inculde_directories("${PROJECT_BINARY_DIR}")

# Add the executalbe 
add_executable(SmartFridge SmartFridge.cpp)